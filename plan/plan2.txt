
**Website specifics (BazaarTable)**
- Fetch `https://api.hypixel.net/skyblock/bazaar` on component mount.
- Parse the response, calculate spreads for all items.
- Show a Material-UI or Tailwind table. Use a custom hook to filter.
- For item icons, use `https://sky.shiiyu.moe/item/<item_id>` or the built‑in Hypixel image URL format: `https://sky.lea.moe/head/<item_id>`.
- Add sorting on profit and margin.
- State managed with React hooks.

**Bot specifics (bot.py)**
- Uses `requests` and `time`; send Discord webhook payload using `json` module.
- For Bazaar, it's straightforward.
- For Auction House, use `https://api.hypixel.net/skyblock/auctions` (no key needed) and only process the first 2 pages (newest auctions). Cache reference prices by periodically fetching SkyCoflnet's median (can be a static file in the repo that gets updated manually, or fetch once per run and tolerate slight staleness).
- Format the Discord embeds with color, fields, and a footer.
- Include a simple `/viewauction` command line for AH snipes.

**Deployment instructions**
- At the end, provide a step‑by‑step guide:
  1. Push repo to GitHub.
  2. Set `DISCORD_WEBHOOK_URL` secret.
  3. Vercel: import the `website/` folder and deploy.
  4. GitHub Actions will start running on schedule automatically.

## Important: No premium APIs, no server cost. Everything must work indefinitely on free tiers (Vercel Free, GitHub Free).

Now produce the complete code for every file. Ensure it is production‑ready and thoroughly commented.